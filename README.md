# CRUD
A Student Management System built using Python and Django with HTML and CSS for the frontend. It supports adding, updating, viewing, and deleting student records. The project has a clean structure and is ideal for learning full-stack development with basic CRUD operations.
